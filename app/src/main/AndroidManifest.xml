<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.zxos.myapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".agera.demo1.AgeraDemo1Activity" />
        <activity android:name=".agera.demo2.AgeraDemo2Activity" />
        <activity android:name=".ndk.NdkDemo1Activity" />
        <activity android:name=".other.demo1.OtherDemo1Activity" />
        <activity android:name=".handle.HandleDemo1Activity" />
        <activity android:name=".customUI.CustomDemo1Activity"></activity>
    </application>

</manifest>